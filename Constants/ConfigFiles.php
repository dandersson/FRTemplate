<?php

namespace FRTemplate\Constants;

/**
 * Define some needed static paths. This is preferably handled in the
 * configuration if not truly needed here.
 */
class ConfigFiles
{
    const SITE = 'config/site.ini.php';
    const DB = 'config/db.ini.php';
}
